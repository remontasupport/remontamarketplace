{
  "name": "remonta",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "quality": "turbo run quality",
    "web": "pnpm --filter @remonta/web",
    "app": "pnpm --filter @remonta/app"
  },
  "devDependencies": {
    "turbo": "^2.10.12"
  }
}
