{
  "$schema": "https://turbo.build/schema.json",

  "//": "Turborepo task graph. One package today; apps/ and packages/ arrive in U6 and U7, and the task definitions below already describe how they will relate, so those units are directory moves rather than configuration rewrites. '^build' means 'build every dependency of this package first' -- a no-op with one package, but it is what orders the graph correctly once packages/db and packages/domain-* exist. Caching is keyed on inputs, and the quality baselines are declared as global dependencies so that editing one invalidates the cache rather than returning a stale pass.",

  "globalDependencies": [
    ".npmrc",
    "tsconfig.json",
    ".quality-baseline/**"
  ],

  "globalEnv": [
    "NODE_ENV",
    "VERCEL_ENV"
  ],

  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "env": [
        "AUTH_DATABASE_URL",
        "DIRECT_DATABASE_URL",
        "DATABASE_URL",
        "NEXTAUTH_SECRET",
        "NEXTAUTH_URL",
        "NEXT_PUBLIC_BASE_URL",
        "NEXT_PUBLIC_SANITY_PROJECT_ID",
        "NEXT_PUBLIC_SANITY_DATASET",
        "NEXT_PUBLIC_RECAPTCHA_SITE_KEY"
      ]
    },

    "type-check": {
      "dependsOn": ["^build"],
      "outputs": []
    },

    "type-check:baseline": {
      "dependsOn": ["^build"],
      "outputs": []
    },

    "lint": {
      "outputs": []
    },

    "lint:baseline": {
      "outputs": []
    },

    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },

    "quality": {
      "dependsOn": ["^build"],
      "outputs": []
    },

    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
