// U7 re-export shim. The implementation moved to @remonta/schemas so both
// apps and a future mobile client share one contract (T2=A). This file previously
// held a byte-identical duplicate of the application's copy.
export * from '@remonta/schemas/types'
